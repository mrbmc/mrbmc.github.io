// Portfolio page — filtering and grid animation handled entirely by CSS.
// Radio inputs in the DOM track filter state; sibling selectors collapse
// non-matching cards to width: 0 with a CSS transition.
